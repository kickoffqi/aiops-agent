apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flask-demo-dev
  namespace: argocd
spec:
  project: platform
  source:
    repoURL: <YOUR_GIT_REPO_URL>
    targetRevision: main
    path: apps/flask-demo/chart
    helm:
      valueFiles:
        - ../envs/dev/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
